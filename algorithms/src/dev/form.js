const fs = require('fs');
const input = fs.readFileSync('./dev/stdin').toString().trim().split('\n');
const item = {

}
const process = (item) => {

}
const output = process(item);
console.log(output);